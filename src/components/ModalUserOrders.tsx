import React from 'react'
import { Order, type User } from '../api/orderApi'
import { Dialog } from '@mui/material'
const ModalUserOrders = (orders:Order[]) => {
  return (
    <div>
      <Dialog>
        Teste
      </Dialog>
    </div>
  )
}

export default ModalUserOrders
